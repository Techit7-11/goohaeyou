spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ON_SECRET
            client-secret: ON_SECRET
            scope:
              - email
              - profile
jwt:
  secret-key: ON_SECRET
custom:
  datasource:
    password: ON_SECRET
  data:
    redis:
      password: ON_SECRET
