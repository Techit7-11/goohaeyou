spring:
  datasource:
    url: jdbc:h2:mem:db_test;MODE=MYSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
custom:
  gen:
    dirPath: c:\temp\goohaeyou\gen